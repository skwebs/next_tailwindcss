/** @type {import('next').NextConfig} */
const nextConfig = {
  // basePath:'/next_tailwindcss',
  // basePath:'/nxt2',
  reactStrictMode: true,
  swcMinify: true,
  trailingSlash: true,
}

module.exports = nextConfig
